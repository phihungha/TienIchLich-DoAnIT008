<UserControl x:Class="TienIchLich.Views.SidePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TienIchLich.ViewModels"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <vm:CategoryDisplayColorOptionIdToStringConverter x:Key="CategoryDisplayColorOptionIdToStringConverter" />
        
        <!-- Appearance of a preset category display color option item in a ComboBox -->
        <DataTemplate DataType="{x:Type vm:CategoryDisplayColorOption}" 
                      x:Key="StandardCategoryDisplayColorOptionTemplate">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="9" Height="9" VerticalAlignment="Center"
                          Margin="3" Fill="{Binding HexCode}" />
                <TextBlock VerticalAlignment="Center" Margin="3" 
                           Text="{Binding Id, Converter={StaticResource CategoryDisplayColorOptionIdToStringConverter}}" />
            </StackPanel>
        </DataTemplate>
        
        <!-- Appearance of the custom category display color option item in a ComboBox -->
        <DataTemplate DataType="{x:Type vm:CategoryDisplayColorOption}" 
                      x:Key="CustomCategoryDisplayColorOptionTemplate">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="9" Height="9" VerticalAlignment="Center" Margin="3" 
                         Fill="{Binding ElementName=DisplayColorComboBox, 
                                        Path=DataContext.CustomDisplayColorOption}" />
                <StackPanel Orientation="Vertical">
                    <TextBlock Margin="3">Tùy chọn</TextBlock>
                    <xctk:ColorPicker x:Name="ColorPicker" 
                                      SelectedColor="{Binding ElementName=DisplayColorComboBox,
                                                              Path=DataContext.CustomDisplayColorOption}"
                                      Width="100" Margin="3" HorizontalAlignment="Center" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        
        <vm:CategoryDisplayColorOptionTemplateSelector x:Key="CategoryDisplayColorOptionTemplateSelector" />
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!-- Calendar category listbox -->

        <Border Grid.Row="0" BorderThickness="3" Background="Blue">
            <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="17">
            Logo here
            </TextBlock>
        </Border>
        

        <Grid Grid.Row="1" Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" FontSize="20" Text="Loại lịch" />
                
                <Button Grid.Column="1" Command="{Binding AddCommand}"
                        HorizontalAlignment="Right" 
                        Padding="7,4,7,4" Content="Thêm" />
            </Grid>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding CalendarCategoryVMs}"
                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                      Margin="0,10,0,0" MinRowHeight="30"
                      HeadersVisibility="None" GridLinesVisibility="None" 
                      Background="Transparent" BorderThickness="0"
                      FontSize="14">
                <DataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsDisplayed, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTemplateColumn Width="40">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                    <Ellipse Width="9" Height="9" VerticalAlignment="Center"
                                             Margin="3" Fill="{Binding DisplayColor}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox Name="DisplayColorComboBox"
                                          ItemsSource="{Binding DisplayColorOptions}" 
                                          SelectedItem="{Binding SelectedDisplayColorOption}"
                                          ItemTemplateSelector="{StaticResource CategoryDisplayColorOptionTemplateSelector}"
                                          FontSize="12" Height="25" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Binding="{Binding Name}" Width="*" />
                    
                    <DataGridTemplateColumn Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DeleteCommand}"
                                        Padding="3,1,3,1" Margin="5,0,10,0" Content="Xóa">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                                            <Setter Property="Visibility" Value="Hidden" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type DataGridRow}},
                                                                                                              Path=IsMouseOver}" 
                                                             Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            
        </Grid>

        
    </Grid>
</UserControl>
